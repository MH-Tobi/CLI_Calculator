[project]
name = "calulator"
version = "0.0.1"
description = "Hello CLI"
requires-python = ">=3.14"
dependencies = [
    "click>=8.4",
]

[project.scripts]
hello = "calulator.calulator:cli"

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"